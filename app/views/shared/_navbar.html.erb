<nav class="nav global">
	<div class="container navigation-bar">
		<div class="logo">
			<a href="/">Welance</a>
		</div>
		<div id="mainListDiv" class="main_list">
			<ul class="navlinks">
				<%if user_signed_in?%>
        <li><%= link_to 'Offers', offers_path %></li>
        <%end%>
        <li><%= link_to 'About', about_path %></li>
        <li><%= link_to "Contact", contact_us_path %></li>
        <% unless user_signed_in?%>
        <li><%= link_to 'Sign up', new_user_registration_path %></li>
        <li><%= link_to 'Sign in', new_user_session_path %></li>
        <%else%>
        <span class="navbar-toggler-icon"></span>
        <div class="collapse navbar-collapse show" id="navbar-list-4" style="margin-top: -0.5rem;">
          <ul class="navbar-nav">
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">

               <% if current_user.photo.attached? %>
               <%= cl_image_tag current_user.photo.key, width: 40, height: 40, crop: :fill, class: "rounded-circle" %>
               <% elsif current_user.avatar_url? %>
               <%= image_tag current_user.avatar_url, width: 40, height: 40, crop: :fill, class: "rounded-circle" %>
               <% else %>
               <%= image_tag "avatar_placeholder.png", width: 40, height: 40, crop: :fill, class: "rounded-circle" %>
               <% end %>

             </a>
             <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
<!--           <a class="dropdown-item" href="/users/<%=current_user.id%>/" style="color: darkgray; font-size: 20px;">Dashboard</a>
          <a class="dropdown-item" href="/users/edit" style="color: darkgray; font-size: 20px;">Edit Profile</a>
          <a class="dropdown-item"rel="nofollow" data-method="delete" href="/users/sign_out" style="color:darkgray; font-size: 20px;">Sign out</a>
        -->
        <%= link_to 'Dashboard', user_path(current_user), class: 'dropdown-item' %>
        <%= link_to 'Edit profile', edit_user_registration_path, class: 'dropdown-item' %>
        <%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: 'dropdown-item' %>
      </div>
    </li>
  </ul>
</div>
<%end%>
</ul>
</div>
<span class="navTrigger">
 <i></i>
 <i></i>
 <i></i>
</span>
</div>
</nav>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>

<!-- Function used to shrink nav bar removing paddings and adding black background -->
<script>
	$(window).scroll(function() {
		if ($(document).scrollTop() > 50) {
			$('.nav.global').addClass('affix');
		} else {
			$('.nav.global').removeClass('affix');
		}
	});
</script>
